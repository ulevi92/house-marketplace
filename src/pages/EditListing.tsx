import { useEffect, useState } from "react";

interface EditListingState {
  loading: boolean;
}

const EditListing = () => {
  const [state, setState] = useState<EditListingState>();

  useEffect(() => {}, []);

  return <></>;
};

export default EditListing;
